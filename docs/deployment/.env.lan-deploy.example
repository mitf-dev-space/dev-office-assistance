# LAN deploy credentials — copy to .env.lan-deploy.local (gitignored) and fill in password.
# Load: $env:HELM_SSH_PASSWORD_FILE = "<path-to-this-dir>/.env.lan-deploy.local"

HELM_SSH_HOST=10.100.235.21
HELM_SSH_USER=masarat-admin
HELM_SSH_PASSWORD=

# Optional overrides
# HELM_REMOTE_DIR=~/compose-dev-office-assistance
HELM_LAN_HOST=10.100.235.21
HELM_WEB_PORT=46810
HELM_API_PORT=46811
# HELM_REGISTRY_NAMESPACE=anstwechy
